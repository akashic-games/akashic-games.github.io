"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Emitter = exports.EmitterStatus = void 0;
var Particle_1 = require("./Particle");
var TOLERANCE = 1e-7;
function limit(val, min, max) {
    if (min != null && val < min)
        return min;
    else if (max != null && val > max)
        return max;
    else
        return val;
}
var EmitterStatus;
(function (EmitterStatus) {
    /// 停止。エミットを停止
    EmitterStatus[EmitterStatus["Stop"] = 0] = "Stop";
    /// 動作中。エミットとパーティクルの更新を行う
    EmitterStatus[EmitterStatus["Running"] = 1] = "Running";
    /// ポーズ。エミットとパーティクルの更新を停止
    EmitterStatus[EmitterStatus["Pause"] = 2] = "Pause";
})(EmitterStatus = exports.EmitterStatus || (exports.EmitterStatus = {}));
var Emitter = /** @class */ (function () {
    function Emitter(param) {
        this.status = EmitterStatus.Stop;
        this.gx = param.gx;
        this.gy = param.gy;
        this.interval = param.interval;
        this.activePeriod = param.activePeriod;
        this.delayEmit = param.delayEmit;
        this.maxParticles = param.maxParticles;
        this.numParticlesPerEmit = param.numParticlesPerEmit;
        this.children = param.children || [];
        this.randomFunc = param.randomFunc;
        this.onInitParticleHandlers = [];
        this.onPreUpdateParticleHandlers = [];
        this.initParam = {
            tx: param.initParam.tx,
            txMin: param.initParam.txMin,
            txMax: param.initParam.txMax,
            ty: param.initParam.ty,
            tyMin: param.initParam.tyMin,
            tyMax: param.initParam.tyMax,
            v: param.initParam.v,
            vMin: param.initParam.vMin,
            vMax: param.initParam.vMax,
            tv: param.initParam.tv,
            tvRelIV: param.initParam.tvRelIV,
            tvNTOA: param.initParam.tvNTOA,
            a: param.initParam.a,
            aMin: param.initParam.aMin,
            aMax: param.initParam.aMax,
            angle: param.initParam.angle,
            rz: param.initParam.rz,
            rzMin: param.initParam.rzMin,
            rzMax: param.initParam.rzMax,
            vrz: param.initParam.vrz,
            vrzMin: param.initParam.vrzMin,
            vrzMax: param.initParam.vrzMax,
            tvrz: param.initParam.tvrz,
            tvrzRelIVRZ: param.initParam.tvrzRelIVRZ,
            tvrzNTOA: param.initParam.tvrzNTOA,
            tvrzC: param.initParam.tvrzC,
            arz: param.initParam.arz,
            arzMin: param.initParam.arzMin,
            arzMax: param.initParam.arzMax,
            sx: param.initParam.sx,
            sxMin: param.initParam.sxMin,
            sxMax: param.initParam.sxMax,
            vsx: param.initParam.vsx,
            vsxMin: param.initParam.vsxMin,
            vsxMax: param.initParam.vsxMax,
            asx: param.initParam.asx,
            asxMin: param.initParam.asxMin,
            asxMax: param.initParam.asxMax,
            sy: param.initParam.sy,
            syMin: param.initParam.syMin,
            syMax: param.initParam.syMax,
            vsy: param.initParam.vsy,
            vsyMin: param.initParam.vsyMin,
            vsyMax: param.initParam.vsyMax,
            asy: param.initParam.asy,
            asyMin: param.initParam.asyMin,
            asyMax: param.initParam.asyMax,
            sxy: param.initParam.sxy,
            sxyMin: param.initParam.sxyMin,
            sxyMax: param.initParam.sxyMax,
            vsxy: param.initParam.vsxy,
            vsxyMin: param.initParam.vsxyMin,
            vsxyMax: param.initParam.vsxyMax,
            asxy: param.initParam.asxy,
            asxyMin: param.initParam.asxyMin,
            asxyMax: param.initParam.asxyMax,
            tsx: param.initParam.tsx,
            tsy: param.initParam.tsy,
            tsxy: param.initParam.tsxy,
            alpha: param.initParam.alpha,
            fadeInNT: param.initParam.fadeInNT,
            fadeOutNT: param.initParam.fadeOutNT,
            lifespan: param.initParam.lifespan
        };
        this.userData = param.userData;
        this.particles = [];
    }
    /**
     * エミッタの動作を開始する。
     */
    Emitter.prototype.start = function () {
        this.status = EmitterStatus.Running;
        for (var i = 0; i < this.children.length; i++) {
            this.children[i].start();
        }
    };
    /**
     * エミッタの動作を停止する。
     *
     * すでにエミットされたパーティクルの更新は行われる。
     */
    Emitter.prototype.stop = function () {
        this.status = EmitterStatus.Stop;
        for (var i = 0; i < this.children.length; i++) {
            this.children[i].stop();
        }
    };
    /*
     * エミッタの動作を一時停止する。
     *
     * エミット及びパーティクルの更新を一時停止する。
     */
    Emitter.prototype.pause = function () {
        this.status = EmitterStatus.Pause;
        for (var i = 0; i < this.children.length; i++) {
            this.children[i].pause();
        }
    };
    /*
     * エミッタをリセットする。
     *
     * 放出済みのパーティクルは破棄される。
     */
    Emitter.prototype.reset = function () {
        this.status = EmitterStatus.Stop;
        this.particles = [];
        for (var i = 0; i < this.children.length; i++) {
            this.children[i].reset();
        }
    };
    Emitter.prototype.emitOneAt = function (x, y) {
        if (this.particles.length >= this.maxParticles) {
            return;
        }
        var tx = this.pickParam(this.initParam.tx, 0);
        var txMin = this.pickParam(this.initParam.txMin, undefined);
        var txMax = this.pickParam(this.initParam.txMax, undefined);
        var ty = this.pickParam(this.initParam.ty, 0);
        var tyMin = this.pickParam(this.initParam.tyMin, undefined);
        var tyMax = this.pickParam(this.initParam.tyMax, undefined);
        var v = this.pickParam(this.initParam.v, 0);
        var vMin = this.pickParam(this.initParam.vMin, undefined);
        var vMax = this.pickParam(this.initParam.vMax, undefined);
        var tv = this.pickParam(this.initParam.tv, undefined);
        var tvRelIV = this.pickParam(this.initParam.tvRelIV, undefined);
        var tvNTOA = this.pickParam(this.initParam.tvNTOA, undefined);
        var a = this.pickParam(this.initParam.a, 0);
        var aMin = this.pickParam(this.initParam.aMin, undefined);
        var aMax = this.pickParam(this.initParam.aMax, undefined);
        var angle = this.pickParam(this.initParam.angle, 0);
        var rz = this.pickParam(this.initParam.rz, 0);
        var rzMin = this.pickParam(this.initParam.rzMin, undefined);
        var rzMax = this.pickParam(this.initParam.rzMax, undefined);
        var vrz = this.pickParam(this.initParam.vrz, 0);
        var vrzMin = this.pickParam(this.initParam.vrzMin, undefined);
        var vrzMax = this.pickParam(this.initParam.vrzMax, undefined);
        // 現時点では参照されていない。これらのパラメータのよう不要は後ほどよく考えるとして、
        // 一旦 lint エラーに対応するためコメントアウトする
        // const tvrz = this.pickParam(this.initParam.tvrz, undefined);
        // const tvrzRelIVRZ = this.pickParam(this.initParam.tvrzRelIVRZ, undefined);
        var tvrzNTOA = this.pickParam(this.initParam.tvrzNTOA, undefined);
        var tvrzC = this.pickParam(this.initParam.tvrzC, undefined);
        var arz = this.pickParam(this.initParam.arz, 0);
        var arzMax = this.pickParam(this.initParam.arzMin, undefined);
        var arzMin = this.pickParam(this.initParam.arzMax, undefined);
        var sx = this.pickParam(this.initParam.sx, 1);
        var sxMin = this.pickParam(this.initParam.sxMin, 1);
        var sxMax = this.pickParam(this.initParam.sxMax, 1);
        var vsx = this.pickParam(this.initParam.vsx, 0);
        var vsxMin = this.pickParam(this.initParam.vsxMin, 0);
        var vsxMax = this.pickParam(this.initParam.vsxMax, 0);
        var asx = this.pickParam(this.initParam.asx, 0);
        var asxMin = this.pickParam(this.initParam.asxMin, 0);
        var asxMax = this.pickParam(this.initParam.asxMax, 0);
        var sy = this.pickParam(this.initParam.sy, 1);
        var syMin = this.pickParam(this.initParam.syMin, 1);
        var syMax = this.pickParam(this.initParam.syMax, 1);
        var vsy = this.pickParam(this.initParam.vsy, 0);
        var vsyMin = this.pickParam(this.initParam.vsyMin, 0);
        var vsyMax = this.pickParam(this.initParam.vsyMax, 0);
        var asy = this.pickParam(this.initParam.asy, 0);
        var asyMin = this.pickParam(this.initParam.asyMin, 0);
        var asyMax = this.pickParam(this.initParam.asyMax, 0);
        var sxy = this.pickParam(this.initParam.sxy, 1);
        var sxyMin = this.pickParam(this.initParam.sxyMin, 1);
        var sxyMax = this.pickParam(this.initParam.sxyMax, 1);
        var vsxy = this.pickParam(this.initParam.vsxy, 0);
        var vsxyMin = this.pickParam(this.initParam.vsxyMin, 0);
        var vsxyMax = this.pickParam(this.initParam.vsxyMax, 0);
        var asxy = this.pickParam(this.initParam.asxy, 0);
        var asxyMin = this.pickParam(this.initParam.asxyMin, 0);
        var asxyMax = this.pickParam(this.initParam.asxyMax, 0);
        var tsx = this.pickParam(this.initParam.tsx, undefined);
        var tsy = this.pickParam(this.initParam.tsy, undefined);
        var tsxy = this.pickParam(this.initParam.tsxy, undefined);
        var alpha;
        var fadeInNT = this.pickParam(this.initParam.fadeInNT, 0);
        var fadeOutNT = this.pickParam(this.initParam.fadeOutNT, 1);
        if (fadeInNT !== 0 && fadeOutNT !== 1) {
            alpha = 0;
        }
        else {
            alpha = this.pickParam(this.initParam.alpha, 1);
        }
        var lifespan = this.pickParam(this.initParam.lifespan, 1);
        var cos = Math.cos(angle);
        var sin = Math.sin(angle);
        // resolve parameters depending on initial values.
        if (tvNTOA != null) {
            var tvTOA = lifespan * tvNTOA;
            if (tv) { // target velocity = tv + v
                a = tv / tvTOA;
                if (tv >= 0)
                    vMax = tv + v;
                else
                    vMin = tv + v;
            }
            else if (tvRelIV) { // target velocity = tvRelIV
                // TODO: impl
            }
        }
        if (tvrzC != null && tvrzNTOA != null) {
            var trvz = vrz * tvrzC;
            var trvzTOA = lifespan * tvrzNTOA;
            arz = (trvz - vrz) / trvzTOA;
            if (trvz >= vrz)
                vrzMax = trvz;
            else
                vrzMin = trvz;
        }
        if (tsx) {
            vsx = (tsx - sx) / lifespan;
            vsxMin = sx;
            vsxMax = vsx;
            asx = 0;
            sxMin = sx;
            sxMax = tsx;
        }
        if (tsy) {
            vsy = (tsy - sy) / lifespan;
            vsyMax = vsy;
            asy = 0;
            syMin = sy;
            syMax = tsy;
        }
        if (tsxy) {
            vsxy = (tsxy - sxy) / lifespan;
            vsxMin = sxy;
            vsxyMax = vsxy;
            asxy = 0;
            sxyMin = sxy;
            sxyMax = tsxy;
        }
        var p = new Particle_1.Particle({
            lifespan: lifespan,
            tx: x + tx,
            txMin: txMin,
            txMax: txMax,
            ty: y + ty,
            tyMin: tyMin,
            tyMax: tyMax,
            vx: cos * v,
            vy: sin * v,
            vMin: vMin,
            vMax: vMax,
            ax: cos * a,
            ay: sin * a,
            aMin: aMin,
            aMax: aMax,
            rz: rz,
            rzMin: rzMin,
            rzMax: rzMax,
            vrz: vrz,
            vrzMin: vrzMin,
            vrzMax: vrzMax,
            arz: arz,
            arzMin: arzMin,
            arzMax: arzMax,
            sx: sx,
            sxMin: sxMin,
            sxMax: sxMax,
            vsx: vsx,
            vsxMin: vsxMin,
            vsxMax: vsxMax,
            asx: asx,
            asxMin: asxMin,
            asxMax: asxMax,
            sy: sy,
            syMin: syMin,
            syMax: syMax,
            vsy: vsy,
            vsyMin: vsyMin,
            vsyMax: vsyMax,
            asy: asy,
            asyMin: asyMin,
            asyMax: asyMax,
            sxy: sxy,
            sxyMin: sxyMin,
            sxyMax: sxyMax,
            vsxy: vsxy,
            vsxyMin: vsxyMin,
            vsxyMax: vsxyMax,
            asxy: asxy,
            asxyMin: asxyMin,
            asxyMax: asxyMax,
            alpha: alpha,
            fadeInNT: fadeInNT,
            fadeOutNT: fadeOutNT
        });
        for (var i = 0; i < this.onInitParticleHandlers.length; i++) {
            this.onInitParticleHandlers[i](p, this);
        }
        this.particles.push(p);
    };
    Emitter.prototype.emitAt = function (x, y) {
        for (var i = 0; i < this.numParticlesPerEmit; i++) {
            this.emitOneAt(x, y);
        }
    };
    /**
     * Emitter.interval間隔でエミットする。
     *
     * @param time Emitterの現在時刻。０以上の実数
     * @param dt 前回のエミットからの経過時間。０より大きい実数
     * @param x エミットするX座標
     * @param y エミットするY座標
     */
    Emitter.prototype.emitTimerAt = function (time, dt, x, y) {
        if (this.activePeriod === 0) {
            return;
        }
        if (this.status !== EmitterStatus.Running) {
            return;
        }
        if (this.particles.length >= this.maxParticles) {
            return;
        }
        if (time < this.delayEmit - TOLERANCE) {
            return;
        }
        time -= this.delayEmit;
        var interval = this.interval;
        var t1 = time;
        var t0 = t1 - dt;
        var ti0 = ((t1 / interval | 0) + 1) * interval;
        var tiN = Math.max((t0 / interval | 0) * interval, 0);
        var activePeriod = this.activePeriod > 0 ? this.activePeriod : 60 * 60 * 60 * 24 * 7;
        if (dt === 0) {
            var isActive = t1 < activePeriod - TOLERANCE;
            if (isActive && (ti0 - TOLERANCE <= t1 || t1 <= tiN + TOLERANCE)) {
                this.emitAt(x, y);
            }
        }
        else {
            for (var ti = ti0; ti >= tiN; ti -= interval) {
                if (ti >= activePeriod - TOLERANCE)
                    continue;
                var ts = ti - TOLERANCE;
                var te = ti + TOLERANCE;
                if (t0 < ts && (t1 > te || (ts <= t1 && t1 <= te))) {
                    this.emitAt(x, y);
                }
            }
        }
    };
    Emitter.prototype.update = function (dt) {
        if (this.status === EmitterStatus.Pause) {
            return;
        }
        this.particles = this.particles.filter(function (p) {
            p.elapse += dt;
            return p.elapse <= p.lifespan;
        });
        for (var i = 0, len = this.particles.length; i < len; i++) {
            var p = this.particles[i];
            for (var j = 0; j < this.onPreUpdateParticleHandlers.length; j++) {
                this.onPreUpdateParticleHandlers[j](p, this, dt);
            }
            p.vx += (this.gx + p.ax) * dt;
            p.vy += (this.gy + p.ay) * dt;
            if (p.vMin != null || p.vMax != null) {
                var v2 = p.vx * p.vx + p.vy * p.vy;
                if (p.vMax != null && v2 > p.vMax * p.vMax) {
                    var v = Math.sqrt(v2);
                    p.vx = p.vx / v * p.vMax;
                    p.vy = p.vy / v * p.vMax;
                }
                else if (p.vMin != null && v2 < p.vMin * p.vMin) {
                    var v = Math.sqrt(v2);
                    p.vx = p.vx / v * p.vMin;
                    p.vy = p.vy / v * p.vMin;
                }
            }
            p.tx = limit(p.tx + p.vx * dt, p.txMin, p.txMax);
            p.ty = limit(p.ty + p.vy * dt, p.tyMin, p.tyMax);
            p.vrz = limit(p.vrz + p.arz * dt, p.vrzMin, p.vrzMax);
            p.rz = limit(p.rz + p.vrz * dt, p.rzMin, p.rzMax);
            p.vsx = limit(p.vsx + p.asx * dt, p.vsxMin, p.vsxMax);
            p.sx = limit(p.sx + p.vsx * dt, p.sxMin, p.sxMax);
            p.vsy = limit(p.vsy + p.asy * dt, p.vsyMin, p.vsyMax);
            p.sy = limit(p.sy + p.vsy * dt, p.syMin, p.syMax);
            p.vsxy = limit(p.vsxy + p.asxy * dt, p.vsxyMin, p.vsxyMax);
            p.sxy = limit(p.sxy + p.vsxy * dt, p.sxyMin, p.sxyMax);
            if (p.fadeInNT !== 0 && p.fadeOutNT !== 1) {
                var t = p.elapse / p.lifespan;
                if (t < p.fadeInNT) {
                    p.alpha = t / p.fadeInNT;
                }
                else if (t > p.fadeOutNT) {
                    p.alpha = 1 - (t - p.fadeOutNT) / (1 - p.fadeOutNT);
                }
                else {
                    p.alpha = 1;
                }
            }
            for (var j = 0; j < this.children.length; j++) {
                this.children[j].emitTimerAt(p.elapse, dt, p.tx, p.ty);
            }
        }
        for (var j = 0; j < this.children.length; j++) {
            this.children[j].update(dt);
        }
    };
    Emitter.prototype.pickParam = function (p, defaultValue) {
        if (!p) {
            return defaultValue;
        }
        else if (p.length === 1) {
            return p[0];
        }
        else {
            var t = this.randomFunc();
            return t * p[0] + (1 - t) * p[1];
        }
    };
    return Emitter;
}());
exports.Emitter = Emitter;
