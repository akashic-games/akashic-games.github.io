import Volume = require("./Volume");
import AABB = require("./AABB");
import Vector2 = require("./Vector2");
/**
 * CircleVolume。
 *
 * アタリ判定用真円形Volume。
 */
declare class CircleVolume implements Volume {
    /**
     * 位置（ワールド座標）。
     */
    pos: Vector2;
    /**
     * 半径。
     */
    r: number;
    aabbFirst: boolean;
    dirty: boolean;
    private _aabb;
    constructor();
    aabb(): AABB;
}
export = CircleVolume;
