/**
 * フォントのウェイト。
 * @deprecated 非推奨である。将来的に削除される。代わりに `FontWeightString` を利用すること。
 */
export declare enum FontWeight {
    /**
     * 通常のフォントウェイト。
     */
    Normal = 0,
    /**
     * 太字のフォントウェイト。
     */
    Bold = 1
}
