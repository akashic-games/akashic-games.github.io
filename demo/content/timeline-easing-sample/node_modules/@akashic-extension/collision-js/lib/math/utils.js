"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.overlap = exports.clamp = void 0;
/**
 * 指定された範囲に数値を制約する。
 *
 * @param val 制約したい数値。
 * @param min 制約の下限。
 * @param max 制約の上限。
 */
function clamp(val, min, max) {
    return Math.max(min, Math.min(max, val));
}
exports.clamp = clamp;
/**
 * オーバーラップテスト。
 *
 * ２つの区間が重なっている時、真。
 *
 * @param aMin 区間Aの最小値。
 * @param aMax 区間Aの最大値。
 * @param bMin 区間Bの最小値。
 * @param bMax 区間Bの最大値。
 */
function overlap(aMin, aMax, bMin, bMax) {
    return !(bMax < aMin || aMax < bMin);
}
exports.overlap = overlap;
